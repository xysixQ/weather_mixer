# Weather Mixer 项目上下文

最后更新：2026-08-24
当前版本：`1.2.30`（`versionCode 87`）
应用包名：`com.weathermixer.sixq`

## 1. 项目定位

Weather Mixer 是一款以多天气源融合为核心的 Android 天气应用。界面使用 Jetpack Compose 和 Material 3，整体交互与信息组织参考 Breezy Weather，但保留独立的动态背景、天气融合、用户画像建议、城市管理和卡片布局编辑能力。

核心目标：

- 国内城市优先快速显示小米天气，再用其他返回较慢的数据源进行加权融合。
- 国外城市按地区自动选择合适数据源，例如美国地区提高 NWS 权重并排除国内源。
- 首页兼顾高刷新率动画、天气信息密度、可配置模块顺序和卡片详情。
- 用户配置、城市、缓存和模块顺序在重启后保留。
- API Key、专属 API Host 和默认 UA 不直接展示在天气源配置输入框中。
- 上传 Git 前必须保持 `local.properties` 未跟踪；天气源默认 Key/Host 只允许通过 `BuildConfig` 从本机 `local.properties` 注入，源码和文档中不能提交真实值。

## 2. 技术结构

- UI：Jetpack Compose、Material 3。
- 架构：MVI，入口位于 `mvi/WeatherApp.kt` 和 `mvi/WeatherContract.kt`。
- 网络：OkHttp。
- 最低 Android：API 24。
- 编译/目标 SDK：37。
- Java/Kotlin JVM Target：11。
- 主 Activity：`MainActivity.kt`，负责 Compose 页面和动画渲染。
- 本地存储：`data/LocalStores.kt`。
- 数据请求与缓存：`data/WeatherRepository.kt`。
- 数据解析：`data/WeatherParsers.kt`。
- 融合与个性化建议：`domain/WeatherFusionEngine.kt`。
- 天气模型：`domain/model/WeatherModels.kt`。
- 首页：`ui/dashboard/`。
- 通用卡片：`ui/components/DashboardCards.kt`。
- 设置：`ui/settings/SettingsScreens.kt`。
- 小工具与 Debug 测试页：`ui/tools/`。

源码目录仍保留历史路径 `com/example/myapplication`，但 Gradle namespace 和安装包名已经改为 `com.weathermixer.sixq`。后续如迁移源码包路径，需要一次性修改 package 声明、测试引用和目录，避免只移动文件导致构建失败。

## 3. 天气源与定位

### 3.1 定位与城市搜索

- 启动时申请系统定位权限，但首次定位使用百度 IP 定位，不再默认北京海淀。
- 使用百度返回的坐标向小米天气查询 location key，并以小米返回结果识别城市，不依赖内置城市库。
- 地区搜索输入后实时返回结果，保留搜索按钮，但无需点击按钮才搜索。
- 选择搜索结果后收起结果；再次点击输入框时重新展开。
- 切换城市后不把城市名自动回填到搜索框。
- 搜索结果显示在“已添加城市”上方；已添加城市为定位区域下方独立区块。
- 国内地点按省、市、县/区顺序展示；国外按当地习惯展示。
- 左上角只显示简短地区名，完整位置显示在大温度下方。
- 同一城市在五分钟内切回时优先使用缓存；刷新请求全局最短间隔为十秒。
- 距离上次刷新不足八分钟时不自动刷新。

### 3.2 数据源策略

- 国内首次加载：小米天气最高优先级，先显示小米结果，再融合其他源。
- 小米在最终融合中的权重略低于最初设计，避免单源过度影响结果。
- 国内温度融合时保留一位小数参与计算，最终首页按整数显示。
- 美国城市启用 NWS 并提高权重，但不设置成压倒性权重。
- 国外地点不查询国内源，避免无效请求触发源自动停用。
- 已移除或停用北京天气、彩云天气、MCN 和 AccuWeather。
- 和风使用新版 API，专属 API Host 从本机 `local.properties` 注入或由用户在设置中填写。
- MSN 已作为天气源加入。
- Meteostat 当前默认启用。该接口需要 RapidAPI 订阅和 `X-RapidAPI-Key`；请求时优先使用 `point/hourly`，如果无有效数据会自动查附近 station 并用 `stations/hourly` 回退测试，两条路径都失败后才记录源失败。
- API 返回错误码且没有有效天气数据时，源会被暂时禁用，并在“上次刷新”下方显示错误信息。
- 默认 Key、Host 和 UA 不回填到配置输入框。Key/Host 留空表示使用本机注入的默认值；填写后才覆盖默认值。

### 3.3 数据能力

当前模型和解析器覆盖：

- 实况温度、体感、最高/最低温。
- 每小时和每日预报。
- 降水概率、降雨量、雨强度。
- 风速、风向、阵风。
- 空气质量、湿度、紫外线、气压。
- 日出、日落、月出、月落和月相。
- 数据源实际返回的露点。
- 天气预警。

露点只融合数据源真实返回值，不使用模型推算。目前已解析 MSN、Visual Crossing、Meteostat、和风和小米的常见露点字段。

天气分类需要尽量保留源数据精度，包括晴、多云、阴、雾、霾、阵雨、雷阵雨、不同雨强、雪、雨夹雪和沙尘暴等。只有数据源确实返回细分类别或雨强时才升级为更精确名称，不凭空推断。

## 4. 首页信息层级

默认模块顺序：

1. 温度主视觉。
2. 每日预报。
3. 逐小时预报。
4. 个性化建议。
5. 预警和其他天气指标。
6. 多元融合分析位于最后。

有预警时，预警位于大温度区域与个性化建议之间。预警标题不携带地区名称，只显示提醒类型；长正文默认收起并可展开。预警使用醒目的半透明分类色。

主页底部显示分割线和 `Made by xysixQ@2026`。

### 4.1 顶部与滚动

- 设置按钮位于右上角，白色/主题适配，不放在卡片中。
- 设置旁有三横线“小工具”按钮，进入小工具页；该页保留两个固定入口，分别显示坤舆航图和 Variflight 航迹。地图类 WebView 已开启第三方 Cookie、地理位置授权、硬件渲染、混合内容和明文资源通道，以兼容第三方地图瓦片与航迹脚本。
- 左上地区按钮平时只包裹文字；温度收起后适当变长并容纳小温度。
- 地区名过长时滚动，首尾带淡入淡出遮罩。
- “上次刷新”位于地区按钮下方，独立于搜索展开动画。
- 大温度居中显示；上滑初段随内容移动，到接近左上按钮后才连续缩小并移动到地区名旁。
- 收起后的温度和地区名基线对齐，不使用两个突然切换的温度副本。
- 下拉刷新时大温度与其他顶部元素使用同一位移通道共同产生弹簧效果。
- 下拉刷新有符合 Material 3 的旋转指示；十秒内重复刷新显示稍后再试提示。

### 4.2 问候语

大温度上方显示结合时间、天气和画像的自然问候，不再只显示天气名。例如：

- 早安、上午好、中午好、下午好、晚上好、晚安。
- 凌晨 1:00 至 5:00 可出现“你还在啊？！（打哈欠）”。
- 根据天气区分正在下雨、天气不错、没有太阳、没有月亮等。
- 晴或多云时可询问是否出门；工作日晚上可结合用户画像给出休息建议。
- 提醒筛选依据未来三小时天气，而非仅依据当前天气。

夜间图标和背景必须按目标城市本地时间及时区切换，不能使用手机所在时区判断洛杉矶等跨时区城市的昼夜。

## 5. 预报与指标卡片

### 5.1 预报图表

- 每日预报覆盖昨天、今天和所有可获得的未来日期。
- 昨日列的文字、图标及对应折线段整体淡化，但不覆盖白色蒙层。
- 每日和逐小时温度曲线使用较粗折线和淡蓝阴影，不显示转折点圆点。
- 温度数字绘制在曲线转折位置。
- 昼夜图标分行并保持水平对齐；没有第二行数据时也不改变第一行纵向位置。
- 每一整列每日预报都可进入该日详情。
- 左右滑动提示使用代码绘制的图标，不依赖系统字体直接显示 `< · >`。

### 5.2 小卡片

- 指标按两列展示，大小卡片具有同等排序权，可互相穿插。
- 降雨与湿度小卡片尺寸一致。
- 降雨卡片保留液体百分比填充，水面受重力影响并有连续波纹。
- 湿度填充区域使用交替短横线纹理；有露点时在左下显示露点。
- 风况使用圆形轮廓和圆角导航箭头，旋转中心与卡片圆心一致。
- 空气质量为圆形仪表，进度颜色随优、良、中、差变化。
- 紫外线使用十二个突起的轮廓，五个等级点位于相邻的下方突起，颜色从绿色逐步到深红。
- 气压使用仪表盘和指针。用力摇晃后指针小幅摆动数次，再逐渐回到实际气压位置。
- 日月升落曲线已走过部分为实线，未走过部分为虚线；天体尚未升起时不在弧线上显示图标。
- 月相位于月亮曲线下方，不单独占卡片。

### 5.3 降雨卡片拉伸动画

- 小卡片拉到两列时，液体从底部先向新增区域流动，再带动高处水体铺开。
- 流动过程中液面可短暂倾斜，最终依据重力方向恢复同一重力势能面。
- 雨伞倾斜后从靠近伞壁开口处倒水，水流为连续、无棱角的弯曲轨迹，起点不能悬空。
- 扩展完成后雨伞补水，液面线性、较慢地恢复原高度。
- 收缩时液面先升高，再平滑回落，接近结束时减速。
- 所有水体几何和动画都不能出现块状搬移或尖锐棱角。

## 6. 卡片详情与动画

- 每张卡片都可点击进入详情，并在详情顶部展示该指标的用途介绍。
- 详情介绍覆盖预警、建议、预报、降水、风、AQI、紫外线、湿度与露点、气压、天文、来源和融合分析。
- 详情底部标注信息来源。
- 点击动画从原卡片圆角矩形非线性扩张为大圆角详情卡，保持原透明度。
- 详情卡不完全铺满屏幕，与左右、底部及顶部地区按钮保持一致边距。
- 详情打开后隐藏其余卡片，避免半透明背景下文字叠加。
- 返回时容器缩回原卡片，文字和图案先淡出，不做上下压缩。
- 预测性返回方向必须与手势边缘一致，且退出期间不出现白底或残留白框。

## 7. 卡片排序与缩放

- 首页和设置中的模块顺序编辑共用同一份持久化数据。
- 大卡片和小卡片可任意混排，不能维护两套相互覆盖的顺序。
- 长按整张卡片进入拖动，不使用固定的右上拖动按钮。
- 长按后松手只进入/保持编辑态，不进入详情；只有普通点击进入详情。
- 拖动卡片时，原位置持续保留占位，禁止后方卡片提前补位造成重复卡片。
- 目标索引根据手指所在卡片直接计算，允许先从 3 拖到 6，再不松手拖回 4。
- 从 2 移到 4 时，只有目标范围内的 3、4 依次前移，目标之后卡片不应被错误移动。
- 大小卡片跨越时，目标卡片必须让位，手指停下处保留插入位置。
- 拖动主手指不松开时，允许第二根手指滚动页面。
- 靠近顶部/底部边缘时自动滚动，速度连续且随靠近边缘略微增加，不能突然跳到顶部或底部。
- 长按进入编辑态时震动；路过每个模块并发生重排时提供短促、轻柔的 `ReorderBuzz`。

小卡片缩放规则：

- 长按后显示贴附卡片的细框线和右下角把手。
- 把手固定在右下角，两个横杠与框线齐平。
- 左边框和上边框不动，只改变右边框和下边框。
- 右侧卡片向外拉伸时不能因为临时宽度跳到下一行。
- 接近展开/收缩阈值时松手，应吸附到对应状态。
- 非法方向拉动或超过尺寸上限时，卡片和框线提供轻微弹性反馈。
- 点击或滑动框线外区域的第一下只退出编辑态，不触发该位置原本操作。

## 8. 动态背景与图标

- 动态背景采用即时几何绘制与预绘制资源结合的方式，目标为 120 Hz。
- 不使用循环播放感明显的壁纸视频。
- 背景有重力感应，变化应明显但不能造成大幅晃动或掉帧。
- 多云背景由较大的几何云随机组合。
- 昼夜、天气图标和背景共同依据城市本地时间更新。
- 晴天太阳需要明亮；夜间晴朗图标在雨天或深色背景上也必须清晰。
- 日月曲线上的图标使用彩色版本，文字前使用单独绘制的黑白图标。
- App 图标支持明暗主题、无系统白边，主体简洁且视觉重心居中。

## 9. 设置与导航

设置项包括：

- 用户画像。
- 主题模式。
- 温度单位。
- 震动反馈。
- 天气源配置。
- 首页模块顺序。
- 机动车限行信息。
- 语言：可自定义 UI 可见字符串，支持导出 JSON 或导入他人语言文件；导入后需重启 App 生效。
- 关于应用：位于语言下方，显示版本、作者和 SDK / 服务列表。

用户画像、关于、天气源配置、模块顺序和其他设置子页面返回后必须回到设置页，不直接回首页。所有二级页面带左上返回箭头，并支持方向正确的预测性返回。

### 9.1 温度单位与主题模式

- 温度单位支持摄氏度、华氏度和开氏度。
- 主题模式支持太阳（浅色）、月亮（深色）和地球（跟随系统）。
- 两者都使用居中的滑动组件，可点击两侧箭头切换。
- 滑动文字在中间受阻尼约束，不随手指移动到边缘。
- 手势位移和动画跟随拖动速度、距离，松手后释放。
- 长按可设置滑动方向是否反向。
- 温度单位和主题模式的反向设置相互独立。
- 主题模式控件不使用设置页普通上浮动画。
- 两者使用由中间密集、向两侧平滑变疏的刻度震感，并适配 ColorOS O-Haptics 可用能力。

### 9.2 用户画像与建议

- 用户画像页面返回设置页。
- 画像只发生变化时，仅重新计算个性化建议，不重新请求天气。
- 驾车出行且打开机动车限行开关时，限行提醒位于个性化建议顶部。
- 限行数据使用本地汇总，不调用在线限行 API。
- 限行卡片使用很淡的半透明黄色。
- 宅家画像不应收到户外工作者补涂防晒提醒。
- 学生防晒建议只能使用学生/上学场景措辞。
- 户外工作、步行、骑行、驾车、花粉、孢子和紫外线建议必须由对应画像与实际天气共同触发。
- 不应因为选择“紫外线敏感”就自动生成花粉或孢子提醒。

## 10. 震动规范

- 设置中有全局震动反馈开关。
- 主要操作和进入新界面时提供明显反馈。
- 温度与主题滑动使用刻度式 `tick` 感，靠近中心密集、向外稀疏，整体偏柔和而非单次清脆点击。
- 模块拖动长按时一次强反馈；每跨越并置换一个模块时提供短促密集反馈。
- 首页小卡片位置变化也必须反馈。
- 按压阴影、涟漪和变深区域都要遵循组件圆角。

## 11. Debug 测试页

- 仅 Debug 构建可进入。
- 长按右上设置按钮满五秒进入；不足五秒按原行为处理，不能提前进入测试页。
- 页面标题为“测试页（仅供 Debug 使用）”。
- 覆盖开关位于大标题下方。
- 打开覆盖时记录当前数据，可手动选择或输入天气代码、时间及时区。
- 关闭覆盖后恢复记录的数据，不立即自动刷新。
- 测试页用于检查天气图标、背景、昼夜、预警场景及时区分支，不用于普通设置。
- 测试页预警场景覆盖无预警、暴雨、高温、雷暴大风、台风、寒潮、大雾和道路结冰，用于回归首页预警卡、动态背景预警等级和详情内容。

## 12. 主题与适配

- 所有页面同时适配浅色和深色模式。
- 浅色模式下左上地区文字为黑色，地区按钮背景保持原有半透明主题色。
- 无卡片背景包裹的文字需根据天气背景亮度自动切换黑白颜色。
- 手机端布局保持现有信息结构。
- 平板端采用左右分栏：左侧较窄区域保持手机首页，右侧显示详情；未选择详情时显示居中的图形化笑脸占位，不能直接用文字字符替代设计图标。

## 13. 性能与启动

- 目标支持 120 Hz，动画时长不能通过假定 60 Hz 帧数实现。
- 使用基于时间的 Compose 动画和绘制，避免按帧创建大量对象。
- 动态天气图标优先使用预绘制帧，降低运行时生成开销。
- 主 Activity 启用硬件加速，主 UI 线程使用显示优先级。
- 网络、缓存读写和 JSON 解析在 IO Dispatcher 执行。
- 已定位的启动卡顿重点是：首次组合/刷新时在主线程读取并解析完整天气缓存。
- 当前实现已把缓存加载和保存迁移到 IO，并增加 `WeatherStartup` Debug 日志记录缓存解码及首帧耗时。
- 不额外拆分多个 Activity 来“提高动画优先级”。Activity 是生命周期和导航边界，拆分会增加创建与转场成本；当前由专用 `MainActivity` 承载渲染，并直接提高 UI 线程优先级。

## 14. 刷新、缓存与离线

- 手动刷新最短间隔十秒，退出重进也沿用同一时间戳。
- 十秒内再次刷新时显示“稍后再试”类提示。
- 自动刷新间隔至少八分钟。
- 城市缓存五分钟内可直接复用，切换回来时不重复请求。
- 新城市先展示小米天气，再把后续融合差异从当前数值平滑过渡，不从零重新播放。
- 首次进入应用的数字从 0/00 平滑变化一次，不能重复播放两轮。
- 融合更新只按旧值到新值过渡，最后几位稍慢。
- 网络不可用时显示离线提示，并尽可能保留上次缓存天气。

## 15. 最近一次实现与验证

最近一次功能批次完成了：

- 根目录新增 `README.md`，说明项目定位、主要功能、本地配置、构建验证命令和仓库注意事项。

- 小米天气和 MSN 天气恢复为无 Key 默认源；移除错误的 appKey/sign/apiKey 必填逻辑，并迁移旧默认 endpoint。
- 每日预报和逐小时天气增加快照兜底：真实 forecast 源为空时仍生成保守趋势列表，避免首页两张卡片消失；新增单元测试覆盖该路径。
- 恢复每日预报和逐小时天气卡片的数据链路：Open-Meteo 现在作为真实无密钥兜底源请求并解析 daily/hourly、风速、湿度、紫外线、日出日落等字段，并迁移旧内置 endpoint。
- 小工具页航图改为本地 HTML 直接加载坤舆 SDK，航图和 Variflight 航迹 WebView 均关闭原生页面缩放，把双指缩放交给网页地图本身处理。
- Variflight 航迹保留桌面 UA，并启用 wide viewport / overview 以优先进入完整网页布局。
- 主页模块顺序页降低滚动卡顿：置换震动改为短促 `ReorderBuzz`，自动滚动改为 16ms 节奏，非拖动滚动时不再给整列卡片挂重排位移动画。
- 首页长按模块拖动震动接入设置页“震动反馈”开关，手势协程读取最新开关状态，并优先使用系统触感反馈再回退到硬件振动，改善三星等设备长按无反馈。
- 首页卡片置换阈值降低灵敏度，减少大小卡片交界处抖动。
- 详情页容器改为真实尺寸动画，避免字体被缩放压扁；关闭详情时延迟恢复底层卡片以缓解闪烁，并保留左上温度。
- 主题切换增加从右上角扩散的揭示动画；温度单位控件文案和布局与主题模式控件统一。
- 每日详情支持左右滑动切换日期，逐小时卡片支持按小时进入详情并显示风速信息。
- 主要卡片不透明度提高，暗色模式背景进一步压低。
- 小工具页保留两个固定入口：航图入口使用本地 HTML 直连坤舆 SDK，航迹入口加载 Variflight ADS-B 页面 `https://flightadsb.variflight.com/#/home`，WebView 将双指缩放交给网页地图处理。
- Variflight 航迹 WebView 补充桌面 UA、第三方 Cookie、地理位置、硬件渲染、wide viewport / overview 和 mixed content/cleartext 支持，尽量避免只加载页面外壳不渲染地图。
- 离线模式缓存提示提升到大温度之上的图层。
- 语言导入限制为 JSON 文件类型，语言页列表行距收紧。
- 关于页移动到语言入口下方，显示作者 xysixQ，移除旧的数据来源说明段，并增加 SDK / 服务列表。
- 主题切换揭示动画改为先覆盖旧主题再切换状态，避免先闪一下再播放动画。
- 主题模式切到“跟随系统”或其他模式时，只有实际明暗外观改变才播放右上角扩散动画；切换后仍是同一明暗外观时只保存模式不播放动画。
- 首页/设置页模块重排震动缩短并降低强度；降雨小卡片小雨伞倒水期间增加低振幅持续震动。
- Meteostat 默认启用和 `point/hourly` → `stations/hourly` 回退。
- 设置页底部增加语言页，可编辑、导出和导入 UI 字符串替换文件。
- Debug 测试页增加多种预警场景入口，可直接注入首页预警卡、动态背景预警等级和详情页内容。
- Debug 页五秒长按和页面标题布局。
- 用户画像建议规则修正及单元测试。
- 露点模型、解析、融合、缓存、首页和详情展示。
- 全部卡片详情介绍。
- 主题模式独立反向滑动设置和震感。
- `3 → 6 → 4` 直接拖动重排、混合尺寸目标判断和双指滚动。
- 详情退出淡化与返回闪白缓解。
- 下拉刷新时大温度同步弹簧位移。
- 启动缓存解析迁移到 IO 和启动性能日志。

在版本 `1.2.30 (87)` 上完成的最后一次完整验证结果：

- `testDebugUnitTest`：31 项测试，0 失败，0 错误。
- `lintDebug`：0 错误/致命问题，50 个非阻塞警告。
- `assembleDebug`：成功。
- 当时 APK SHA-256：`8203446DF31D0F0DF7320D5518F3D21E7AEFFBF555588440260C959DC192CFA1`。

本次修复小米/MSN 被误设为需要 Key、每日/逐小时预报卡片在无真实 forecast 源时消失、首页长按拖动震动未稳定跟随设置开关、主题模式切换无实际明暗变化仍播放扩散动画的问题，并新增 README；升级到 `1.2.30 (87)`，已完成本地完整验证。

## 16. 当前风险与后续验证重点

- 按用户要求，近期不再进行连接手机的实机调试；Compose 手势、120 Hz、重力传感器和预测性返回仍需最终人工体验验证。
- 卡片拖拽是目前风险最高的交互，重点回归：大小卡片混排、边缘滚动、第二指滚动、`3 → 6 → 4`、右侧小卡片扩展和重复卡片。
- 透明详情返回需在浅色/深色、晴天/夜间/雨天背景分别检查闪白。
- 洛杉矶等跨时区城市需验证图标、背景、问候和日月数据使用城市时区。
- Meteostat 只有在用户提供已订阅 RapidAPI 的 Key 后才能做真实可用性验证，不能仅凭本地字符串判断。
- 各数据源错误停用策略应区分鉴权失败、限流、地区不支持和临时网络失败，避免永久停用可恢复源。
- `lintDebug` 仍有非阻塞警告，可在不改变行为的独立批次中处理。

## 17. 常用命令

在项目根目录执行：

```powershell
$env:ANDROID_USER_HOME='D:\andr_appli\.android'
.\gradlew.bat --gradle-user-home D:\andr_appli\.gradle "-Pkotlin.compiler.execution.strategy=in-process" testDebugUnitTest lintDebug assembleDebug
```

Debug APK 输出位置：

```text
D:\andr_appli\app\build\outputs\apk\debug\app-debug.apk
```

启动性能日志标签：

```text
WeatherStartup
```

## 18. 开发约定

- 每次用户要求修改代码或项目文件时递增版本号。
- 不覆盖用户已填写的 API Key、Host 或本地配置。
- 不把默认密钥写入可见输入框、文档、日志或提交记录。
- 修改前先阅读现有 MVI、Store 和 Compose 组件，优先复用同一状态源。
- 动画和拖拽修改必须避免同一元素维护两套位置或两套身份。
- 数据源增加字段时同步检查：模型、解析、融合、缓存序列化、详情来源和测试。
- 页面新增时同步检查：左上返回、预测性返回、主题、震动、平板分栏和详情来源。
- 未进行实机验证时必须明确说明，不能把编译通过等同于触感和帧率验证通过。
